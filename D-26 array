#include<stdio.h>
int main(){
int n,a[100],i,sum=0;
printf("Enter the size of array:");
scanf("%d",&n);
for(i=0;i<n;i++){
    scanf("%d",&a[i]);
}
for(i=0;i<n;i++){
    
if(a[i]<=5){
    sum+=i;
}
}
printf("%d",sum);
return 0;
}
